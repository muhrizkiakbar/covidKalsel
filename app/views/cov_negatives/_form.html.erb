<%= form_with(model: cov_negative, local: true) do |form| %>
  <% if cov_negative.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cov_negative.errors.count, "error") %> prohibited this cov_negative from being saved:</h2>

      <ul>
      <% cov_negative.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :city_id %>
    <%= form.text_field :city_id %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
  </div>

  <div class="field">
    <%= form.label :dateTime %>
    <%= form.datetime_select :dateTime %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
