
<% content_for :isi do %>
  <div class="container-fluid mt--6">
    <p id="notice"><%= notice %></p>

    <div class="card">
      <div class="card-header">
        <h3>Show CoV Died</h3>
      </div>
      <div class="card-body">
        <p>
          <strong>City :</strong>
          <%= @cov_died.city.name %>
        </p>

        <p>
          <strong>Amount :</strong>
          <%= @cov_died.amount %>
        </p>

        <p>
          <strong>Datetime :</strong>
          <%= @cov_died.dateTime %>
        </p>
      </div>
      <div class="card-footer">

        <%= link_to 'Edit', edit_cov_died_path(@cov_died), class: "btn btn-primary" %>

        <%= link_to 'Back', cov_dieds_path, class: "btn btn-secondary" %>

      </div>

    </div>

    
    <br>
    <%# paginate @cities %>



    <%= render partial: 'layouts/footer_back' %>
  </div>
<% end %>

<% content_for :button_header do %>
  <div class="col-lg-6 col-5 text-right">
    <%= link_to 'New Cov Died', new_cov_died_path, class: "btn btn-sm btn-neutral" %>
  </div>
<% end %>
      
<%= render partial: 'layouts/body' %>
