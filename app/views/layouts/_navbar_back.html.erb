<nav class="sidenav navbar navbar-vertical  fixed-left  navbar-expand-xs navbar-light bg-white" id="sidenav-main">
  <div class="scrollbar-inner">
    <!-- Brand -->
    <div class="sidenav-header  d-flex  align-items-center">
      <a class="navbar-brand" href="/">
        <%=image_tag asset_path('img/brand/white.png'), class:"navbar-brand-img", alt:"..."%>
      </a>
      <div class=" ml-auto ">
        <!-- Sidenav toggler -->
        <div class="sidenav-toggler d-none d-xl-block" data-action="sidenav-unpin" data-target="#sidenav-main">
          <div class="sidenav-toggler-inner">
            <i class="sidenav-toggler-line"></i>
            <i class="sidenav-toggler-line"></i>
            <i class="sidenav-toggler-line"></i>
          </div>
        </div>
      </div>
    </div>
    <div class="navbar-inner">
      <!-- Collapse -->
      <div class="collapse navbar-collapse" id="sidenav-collapse-main">
        <!-- Nav items -->
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link <%= controller_name == 'dashboard' ? 'active' : '' %>" href="#navbar-dashboards" data-toggle="collapse" role="button" aria-expanded="true" aria-controls="navbar-dashboards">
              <%= icon("fas", "tachometer-alt", class:"text-primary") %>
              <span class="nav-link-text">Dashboards</span>
            </a>
            <div class="collapse <%= controller_name == 'dashboard' ? 'show' : '' %>" id="navbar-dashboards">
              <ul class="nav nav-sm flex-column">

                  <li class="nav-item">
                    <%= link_to root_path, class: "nav-link" do %>
                      <span class="sidenav-mini-icon"> D </span>
                      <span class="sidenav-normal"> Dashboard </span>
                    <% end %>
                  </li>

              </ul>
            </div>
          </li>
          <li class="nav-item">
            <% @cov = ["cov_dieds", "cov_pdps", "cov_odps", "cov_recovereds", "cov_negatives", "cov_positives", "cov_pdp_processeds", "cov_odp_processeds"] %>
            <a class="nav-link <%= @cov.include?(controller_name) ? 'active' : '' %>" href="#navbar-examples" data-toggle="collapse" role="button" aria-expanded="false" aria-controls="navbar-examples">
              <%= icon("far", "clone", class: "text-orange") %>
              <span class="nav-link-text">CoV</span>
            </a>
            <div class="collapse <%= @cov.include?(controller_name) ? 'show' : '' %>" id="navbar-examples">
              <ul class="nav nav-sm flex-column">
                <% if policy(:cov_positive).index? %>
                  <li class="nav-item">
                    <%= link_to cov_positives_path, class: "nav-link" do %>
                      <span class="sidenav-mini-icon"> P </span>
                      <span class="sidenav-normal"> Positif </span>
                    <% end %>
                  </li>
                <% end %>
                <% if policy(:cov_recovered).index? %>
                  <li class="nav-item">
                    <%= link_to cov_recovereds_path, class: "nav-link" do %>
                      <span class="sidenav-mini-icon"> S </span>
                      <span class="sidenav-normal"> Sembuh </span>
                    <% end %>
                  </li>
                <% end %>
                <% if policy(:cov_died).index? %>
                  <li class="nav-item">
                    <%= link_to cov_dieds_path, class: "nav-link" do %>
                      <span class="sidenav-mini-icon"> M </span>
                      <span class="sidenav-normal"> Meninggal </span>
                    <% end %>
                  </li>
                <% end %>
                <% if policy(:cov_negative).index? %>
                  <li class="nav-item">
                    <%= link_to cov_negatives_path, class: "nav-link" do %>
                      <span class="sidenav-mini-icon"> N </span>
                      <span class="sidenav-normal"> Negatif </span>
                    <% end %>
                  </li>
                <% end %>
                <a href="#navbar-multilevel-odp" class="nav-link" data-toggle="collapse" role="button" aria-expanded="<%= ["cov_odps", "cov_odp_processeds"].include?(controller_name) ? 'true' : 'false' %>" aria-controls="navbar-multilevel-odp">
                  <span class="sidenav-mini-icon"> O </span>
                  <span class="sidenav-normal"> ODP </span>
                </a>
                <div class="collapse <%= ["cov_odps", "cov_odp_processeds"].include?(controller_name) ? 'show' : '' %>" id="navbar-multilevel-odp" style="">
                  <ul class="nav nav-sm flex-column">
                    <% if policy(:cov_odp).index? %>
                      <li class="nav-item">
                        <%= link_to cov_odps_path, class: "nav-link" do %>
                          <span class="sidenav-normal"> Tambah </span>
                        <% end %>
                      </li>
                    <% end %>
                    <% if policy(:cov_odp_processed).index? %>
                      <li class="nav-item">
                        <%= link_to cov_odp_processeds_path, class: "nav-link" do %>
                          <span class="sidenav-normal"> Kurang </span>
                        <% end %>
                      </li>
                    <% end %>
                  </ul>
                </div>
                <a href="#navbar-multilevel-pdp" class="nav-link" data-toggle="collapse" role="button" aria-expanded="<%= ["cov_pdps", "cov_pdp_processeds"].include?(controller_name) ? 'true' : 'false' %>" aria-controls="navbar-multilevel-pdp">
                  <span class="sidenav-mini-icon"> P </span>
                  <span class="sidenav-normal">PDP </span>
                </a>
                <div class="collapse <%= ["cov_pdps", "cov_pdp_processeds"].include?(controller_name) ? 'show' : '' %>" id="navbar-multilevel-pdp" style="">
                  <ul class="nav nav-sm flex-column">
                    <% if policy(:cov_pdp).index? %>
                      <li class="nav-item">
                        <%= link_to cov_pdps_path, class: "nav-link" do %>
                          <span class="sidenav-normal"> Tambah </span>
                        <% end %>
                      </li>
                    <% end %>
                    <% if policy(:cov_pdp_processed).index? %>
                      <li class="nav-item">
                        <%= link_to cov_pdp_processeds_path, class: "nav-link" do %>
                          <span class="sidenav-normal"> Kurang </span>
                        <% end %>
                      </li>
                    <% end %>
                  </ul>
                </div>
              </ul>
            </div>
          </li>
          <li class="nav-item">
            <% @informasi = ["hospitals","telegram_chat", "information", "info_practices", "cities", "letters"] %>
            <a class="nav-link <%= @informasi.include?(controller_name) ? 'active' : '' %>" href="#navbar-components" data-toggle="collapse" role="button" aria-expanded="false" aria-controls="navbar-components">
              <%= icon("far", "hospital", class:"text-info") %>
              <span class="nav-link-text">Informasi Umum</span>
            </a>
            <div class="collapse <%= @informasi.include?(controller_name) ? 'show' : '' %>" id="navbar-components">
              <ul class="nav nav-sm flex-column">
                <% if policy(:hospital).index? %>
                  <li class="nav-item">
                    <%= link_to hospitals_path, class: "nav-link" do %>
                      <span class="sidenav-mini-icon"> RS </span>
                      <span class="sidenav-normal"> Rumah Sakit </span>
                    <% end %>
                  </li>
                <% end %>
                <% if policy(:information).index? %>
                  <li class="nav-item">
                    <%= link_to information_index_path, class: "nav-link" do %>
                      <span class="sidenav-mini-icon"> I </span>
                      <span class="sidenav-normal"> Informasi </span>
                    <% end %>
                  </li>
                <% end %>
                <% if policy(:info_practice).index? %>
                  <li class="nav-item">
                    <%= link_to info_practices_path, class: "nav-link" do %>
                      <span class="sidenav-mini-icon"> G </span>
                      <span class="sidenav-normal"> Gambar </span>
                    <% end %>
                  </li>
                <% end %>
                <% if policy(:letter).index? %>
                  <li class="nav-item">
                    <%= link_to letters_path, class: "nav-link" do %>
                      <span class="sidenav-mini-icon"> S </span>
                      <span class="sidenav-normal"> Surat </span>
                    <% end %>
                  </li>
                <% end %>
                <% if policy(:city).index? %>
                  <li class="nav-item">
                    <%= link_to cities_path, class: "nav-link" do %>
                      <span class="sidenav-mini-icon"> K </span>
                      <span class="sidenav-normal"> Kabupaten/Kota </span>
                    <% end %>
                  </li>
                <% end %>
                <% if policy(:telegram_chat).index_chat? %>
                  <li class="nav-item">
                    <%= link_to telegram_chat_by_username_path, class: "nav-link" do %>
                      <span class="sidenav-mini-icon"> T </span>
                      <span class="sidenav-normal"> Telegram Chat By User </span>
                    <% end %>
                  </li>
                <% end %>
              </ul>
            </div>
          </li>
          <li class="nav-item">
            <% @pengguna = ["users", "roles"] %>
            <a class="nav-link <%= @pengguna.include?(controller_name) ? 'active' : '' %>" href="#navbar-forms" data-toggle="collapse" role="button" aria-expanded="false" aria-controls="navbar-forms">
              <%= icon("fas", "users", class:"text-pink") %>
              <span class="nav-link-text">Users</span>
            </a>
            <div class="collapse <%= @pengguna.include?(controller_name) ? 'show' : '' %>" id="navbar-forms">
              <ul class="nav nav-sm flex-column">
                <% if policy(:user).index? %>
                  <li class="nav-item">
                    <%= link_to users_path, class: "nav-link" do %>
                      <span class="sidenav-mini-icon"> U </span>
                      <span class="sidenav-normal"> User </span>
                    <% end %>
                  </li>
                <% end %>
                <% if policy(:role).index? %>
                  <li class="nav-item">
                    <%= link_to roles_path, class: "nav-link" do %>
                      <span class="sidenav-mini-icon"> R </span>
                      <span class="sidenav-normal"> Roles </span>
                    <% end %>
                  </li>
                <% end %>
              </ul>
            </div>
          </li>
        </ul>
      </div>
    </div>
  </div>
</nav>
