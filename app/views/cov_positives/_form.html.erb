<%= form_with(model: cov_positive, local: true) do |form| %>
  <% if cov_positive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cov_positive.errors.count, "error") %> prohibited this cov_positive from being saved:</h2>

      <ul>
      <% cov_positive.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :city_id %>
    <%= form.text_field :city_id %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
  </div>

  <div class="field">
    <%= form.label :dateTime %>
    <%= form.datetime_select :dateTime %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
